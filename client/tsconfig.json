{
    "extends": "../tsconfig_base.jsonc",
    "include": ["src/**/*.ts"],
    "compilerOptions": {
        "lib": ["ES2019"],  // Any features not supported must be converted to utils (no polyfills)
    },
}
