{
    "extends": "../tsconfig_base.jsonc",
    "include": ["src/**/*.ts"],
    "compilerOptions": {
        // Any features not supported must be converted to utils (no polyfills)
        // NOTE DOM just for optional html utilities that can also be used with jsdom
        "lib": ["ES2019", "DOM", "DOM.Iterable"],
    },
}
